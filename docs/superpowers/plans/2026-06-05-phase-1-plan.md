# Implementation Plan: Phase 1 — Scaffold, DB Connection, Index Creation, Settings Init

## Tasks

### Task 1: Environment and Git Setup
- [ ] Create `requirements.txt` listing:
  - `aiogram==3.7.0`
  - `motor==3.4.0`
  - `apscheduler==3.10.4`
  - `python-dotenv==1.0.0`
  - `pymongo==4.6.0`
- [ ] Initialize Python Virtual Environment `.venv` and install all requirements.
- [ ] Update `.gitignore` to exclude `.venv/`, `.env`, and `**/__pycache__/`.
- [ ] Create `.env.example` template file.
- [ ] **Commit Checkpoint:** `chore: initialize virtual environment and git exclusions`

### Task 2: Config and Database Connections
- [ ] Implement `bot/config.py` using `python-dotenv` and add assertions for required fields.
- [ ] Implement `bot/database/db.py` with `connect_db()`, `close_db()`, `create_indexes()`, `init_settings()`, `get_settings()`, and `update_settings()`.
- [ ] **Commit Checkpoint:** `feat: implement configuration and database initialization layer`

### Task 3: Stub Handlers and Tasks
- [ ] Add basic router stubs to `bot/handlers/user.py` and `bot/handlers/admin.py`.
- [ ] Add basic `setup_scheduler()` stub to `bot/scheduler/tasks.py`.
- [ ] **Commit Checkpoint:** `feat: implement handlers and scheduler stubs for initial run`

### Task 4: Main Bot Application Runner
- [ ] Implement `bot/bot.py` with `on_startup`, `on_shutdown`, and the async main polling runner.
- [ ] **Commit Checkpoint:** `feat: implement bot main entrypoint and event handlers`

### Task 5: Verification
- [ ] Write a verification script to validate import integrity and configuration loading.
- [ ] **Commit Checkpoint:** `test: add Phase 1 verification script`
